/**
  This file defines the types compatible to MySQL databases
  Ex:
  STRING.prototype.toSql = function toSql() {
    return 'VARCHAR(' + this._length + ')' + (this._binary ? ' BINARY' : '');
  };

*/
